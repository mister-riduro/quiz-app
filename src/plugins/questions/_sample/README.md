# Sample Question Plugin (`_sample`)

Contoh implementasi standar micro-plugin tipe soal untuk EduPlay.
Setiap plugin baru dapat dibuat dengan membuat folder baru di `src/plugins/questions/<nama-tipe>` dan mengimplementasikan kontrak `QuestionPlugin<TContent, TAnswer>` dari `@/plugins/core`.

